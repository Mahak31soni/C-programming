arr[5] = {4,1,7,6,8}
len = arr.length()
for(int i = 0; i <len-1; i++)
	for(int j = 1; j <len; j++)
		if(arr[i] > arr[j])
			arr[i], arr[j] = arr[j], arr[i];
			j++;
		else
			j++;
	i++;
	
print(arr[5])

Python 
len = arr.len()
for i in range(len - 1):
	for j in range(1, len):
		if arr[i] > arr[j]:
			arr[i], arr[j] = arr[j], arr[i]
			