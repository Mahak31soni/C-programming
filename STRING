#include <iostream>
using namespace std;


int main() {
    
    string str = "virat kholi is the best";
    int end = str.length();
    for(int i = str.length()-1; i>= 0; i--)
    {
        if(str[i] == ' ')
        {
            for(int j = i+1; j< end; j++)
            {
                cout << str[j];
            }
            cout<<" ";
            end = i;
        }
        
    }

    for(int i = 0; i < end; i++)
    {cout << str[i];}
    return 0;
